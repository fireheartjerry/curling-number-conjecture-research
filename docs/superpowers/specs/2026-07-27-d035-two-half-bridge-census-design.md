# D-035 definition-first two-half bridge census

**Date:** 2026-07-27

**Evidence sought:** `COMPUTED` through \(q\le25\). This census is not an
unbounded proof and does not close either boundary word wall, Cell C, either
G2CS target, or the Curling Number Conjecture.

## Purpose and inherited domain

The D-034 transition atlas deliberately records
`bridge_scope=not_enumerated`. D-035 must fill exactly that finite-evidence
gap without reclassifying the sampled \(E/F\) windows.

Retain the exact simultaneous-boundary row

\[
q>2r>0,\qquad P=q+r,\qquad R=BQB,\qquad U=QB,
\]

\[
(z,h_{\rm mis})=(1,0),\qquad
B[0]=2,\qquad Q[0]=3,\qquad\lambda\le1,
\]

and the exact \(p>q\) and \(p<q\) normal forms already generated by
`generated_two_cube_cell_c_z1_atlas.py`. Before inspecting the bridge,
retain exactly the D-034 static filters

\[
(\kappa(R^2),\pi(R^2))=(2,q),\qquad
\kappa(R^2B)=2,\qquad
(\kappa(BRB^2),\pi(BRB^2))=(3,r),
\]

\[
(\kappa(X^3),\pi(X^3))=(3,p),\qquad
(\kappa(X^3UB^2),\pi(X^3UB^2))=(3,r).
\]

Reusing these exact structural generators is intentional. The new behavior
is the bridge trace, and a separate test-side raw-root oracle must audit it
at a bound where both branches have complete local replays.

Here \(z\) is the first position of `2` in \(U\),
\(h_{\rm mis}\) is the first mismatch between \(U\) and the periodic
extension \(X^\mathbb Z\), and \(\lambda\) is the length of the terminal
run of `3`s in \(B\). The inherited coordinate \(h_{\rm mis}\) is not a
bridge phase. Below, bridge phase is always denoted by \(s\), so it cannot
be confused with the target notation \(K_s\).

## Local/full-context and target scope

The executable starts from the finite standalone word
\(G_{\rm loc}=X^3U\). It does **not** choose an arbitrary left context
\(L\), and therefore does not by itself produce full orbit states
\(LR^2B^2[0:s]\). Its successful objects are called *local bridge
replays*, not full-context bridges.

For a capped full-context bridge cut, (C.28) is the separate theorem which
identifies the full-state pair with this local suffix pair. The artifact
must therefore state:

```text
orbit_scope=standalone_G_loc_seed
target_assumption=none
full_context_not_enumerated=true
```

It must also preserve the exact target ledger

\[
\mathcal I\cap\{K_s:0\le s\le2r\}=\{K_0,K_{2r}\},
\qquad
\{K_s:0\le s\le2r\}\subseteq\mathcal J.
\]

The byte-stable ledger spells out the distinct cap hypotheses:

```text
I_bridge_membership=K0_and_K2r_only
J_bridge_membership=all_K0_through_K2r
two_cut_cap=target_independent
interior_three_cut_I_cap=not_automatic
proper_bridge_J_cap=under_J_negation
```

Thus `2`-cut self-caps are target-independent; the
\(\mathcal J\)-negation supplies the cap at every proper full bridge cut;
and an interior `3` cut omitted from \(\mathcal I\) still requires an
independent cap before localization. The census does not classify those
unknown full-context \(\mathcal I\) cuts.

## Definition-first bridge trace

For each retained static model, put

\[
G_{\rm loc}=X^3U,\qquad C=B^2.
\]

At every proper bridge phase \(0\le s<2r\), recompute from the suffix-power
definition

\[
(\kappa_s,\pi_s)
=
(\kappa(G_{\rm loc}C[0:s]),\pi(G_{\rm loc}C[0:s])).
\]

The standalone local trace replays the requested bridge exactly when

\[
\kappa_s=C[s]\qquad(0\le s<2r).
\]

Stop a failed local replay at its first mismatching phase and count that
phase. For a complete local replay, also recompute the endpoint pair after
all of \(B^2\); the static contract requires \((3,r)\).

The normal forms give the literal suffix identities

\[
\operatorname{suf}_{2q+i}(X^3U B[0:i])=R^2B[0:i],
\]

\[
\operatorname{suf}_{2r+i}(X^3U B\,B[0:i])=B^2B[0:i].
\]

A visible proper circular fourth power is a suffix ending at the current
cut:

\[
0<a<q,\quad 4a\le2q+i,\quad
\operatorname{suf}_{4a}(R^2B[0:i])=Y^4
\]

on the first half, and the analogous statement with
\(0<a<r\) and \(B^2B[0:i]\) on the second. Directly enumerate both:

1. **all** fourth-power suffix roots of the full scanned local state;
2. the visible proper-circular subset in the displayed context.

For each, report root-occurrence and cut counts separately.

## Required bounded classifications

For each branch, record:

- structural, \(z=1\), and exact-static assignment counts;
- the first bridge-mismatch phase distribution;
- complete local bridge-replay count and the number with every proper local
  bridge period below \(P\);
- exact endpoint count;
- complete-local-replay distributions by \(r\), by \(B\), and by
  \(p<q\) seam;
- total proper-cut count;
- for each half and requested label, the canonical root relation to the
  relevant scale: \(<q,=q,>q\) on the first half and
  \(<r,=r,>r\) on the second;
- all-full-suffix and visible-proper fourth-power root/cut counts;
- a period-\(P\) cross-tab by half and requested label;
- a second-half `3` cross-tab
  \((\pi<r,\pi=r,\pi>r)\times(\text{seam false},\text{seam true})\);
- separate opportunity and violation counters for every D-035 theorem
  conclusion.

The theorem-audit counters are checked only after a complete standalone
local replay is established. They must cover:

1. the first-half and second-half `2` root ceilings;
2. the capped first-half `3` strict root bound and visible cube;
3. the capped second-half `3` bound and visible low-root cube;
4. the one-way full-root seam, terminal-phase consequence under
   \(\lambda\le1\), and suffix-of-`3`s conclusion;
5. absence of a fourth-power suffix in the full local state, and absence
   of a visible proper fourth power.

Every violation counter must have a corresponding opportunity counter.
The required pairs are: endpoint pair; first/second `2` cap; capped first
`3` bound and visible cube; capped second `3` bound; low second `3` visible
cube; full-root seam, suffix-of-`3`s, and terminal phase; all fourth-power
suffixes; and visible proper fourth powers. A zero opportunity count is
reported rather than passed off as corroboration.

The total proper-cut count is \(\sum 2r\) over complete local replays.
Stage counters state their universes: structural assignments, \(z=1\)
assignments, exact-static candidates, failed local prefixes, complete local
replays, or cuts of complete local replays. No theorem conclusion may be
used as a search filter.

Root-occurrence counters sum the number of qualifying root lengths over
proper cuts; root-cut counters count proper cuts having at least one such
root. Period-\(P\), seam, opportunity, and violation counters are all
cut-level except for the endpoint opportunity, which is once per complete
local replay.

For every second-half `3` cut, compute the seam equation independently of
its canonical root. Audit only

\[
\pi=r\Longrightarrow\text{seam}.
\]

Never filter on the seam and never infer the converse.

## Sharp certificates

Pin six fully recomputable certificates:

1. the least \(p>q\) local replay, at
   \(q=12,r=4,B=\texttt{2232}\);
2. the least \(p<q,\ r=1\) local replay, at
   \(q=8,r=1,B=\texttt{2}\);
3. all four bounded nontrivial \(p<q,\ r=4\) local replays, occurring at
   \(q=23,24,25,25\) with \(B=\texttt{2223}\).

Each certificate is a literal table containing at least branch,
\((p,q,r,P)\), seam type, \(B,Q,X\), every expected cut pair, period-cap
status, every second-half-`3` seam status, and both fourth-power counts.
The two \(q=25\) rows have distinct literal \(Q,X\) words.

These certificates pin realized equality in both `2`-cut ceilings, proper
low-root `3` cuts, the large bounded \(r=1\) family, and every nontrivial
\(p<q\) local replay through \(q\le25\). The four \(p<q,r=4\)
certificates have a true seam but canonical pair \((3,1)\), directly
protecting the frozen non-converse. They are local models, not G2CS
counterexamples.

## Executable interface and artifact

Create `research/generated_two_cube_d035_bridge_census.py` with:

- `trace_bridge`;
- `scan_bridge_census`;
- `known_bridge_certificates`;
- `audit_bridge_certificate`;
- `render_census`;
- a deterministic `--max-q` / `--output` CLI.

Create the byte-stable artifact
`research/outputs/generated_two_cube_d035_bridge_census_2026-07-27.txt`.
It must say `status=COMPUTED`, name the exact bridge-only scope, preserve
`Cell_C=OPEN`, and end with a no-proof warning.

Without `--output`, stdout is exactly the deterministic rendered artifact.
With `--output`, stdout is empty and the file uses LF bytes. Runtime timing
is diagnostic only and is written to stderr.

## Independent tests and RED gate

Before production implementation:

1. import the wished-for interface from a new test module;
2. implement a test-side literal exponent/period oracle;
3. raw-enumerate both normal forms through \(q\le12\), where both branches
   have complete local replays;
4. compare all bridge-stage counts and classifications;
5. pin and independently audit all six certificates;
6. test deterministic rendering and invalid inputs;
7. require the \(q\le25\) artifact and its digest.

The raw oracle may not call the production D-034 generator, static
predicate, witness function, bridge tracer, or certificate selector. It
must reconstruct both normal forms from their equations and use a separate
literal exponent/period loop. Because \(q\le12\) does not reach the
nontrivial \(p<q,r=4\) regime, all four larger certificates must also appear
as independent literal test data and be audited by that separate oracle.

Run the new focused test and retain the expected import failure before
creating the production module.

## Nonclaims

The finite scan cannot:

- prove that the observed \(r=1\), \(r=4\), or \(B\)-patterns persist
  unboundedly;
- prove that zero theorem-audit violations implies the D-035 lemmas;
- turn a localized static word into an arbitrary full-context orbit without
  the cap hypotheses used by the localization theorem;
- classify omitted \(\mathcal I\) `3` cuts globally;
- close either \(p\)-branch, Cell C, G2CS, or the Curling Number Conjecture.
