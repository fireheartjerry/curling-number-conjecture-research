# Cloud continuation prompt

Continue the private repository's attack on the Curling Number Conjecture.

## Final objective

Find a complete affirmative proof for every finite nonempty integer sequence.
Formalize every load-bearing claim in Lean 4. Validate every numerical
curling-number claim with calibrated executed code. Search the literature
freely before rederiving known results. If a disproof exists, provide a
rigorously certified counterexample.

Do not report completion until an adversarial requirement-by-requirement audit
confirms the full theorem, all external hypotheses, the Lean formalization, and
all computational claims.

## Mandatory reading order

1. `README.md`
2. `CURRENT_STATUS.md`
3. `docs/REPOSITORY_AUDIT_2026-07-27.md`
4. `docs/PROVENANCE.md`
5. `external/chatgpt-mega-ledger-2026-07-27/`
   `CURLING_NUMBER_CONJECTURE_MEGA_LEDGER_2026-07-27.md`, Sections 0–3,
   14–23, and 24–32
6. Relevant internal notes under `research/`

The repository audit overrides stale computational labels and the mega-ledger
front matter overrides older appendices.

## First task: repair the immediate theorem statement

Before attempting a proof, write a fully quantified Generated Two-Cube
Synchronization statement using

\[
R=AB=TU,\quad q=|R|,\quad b=|B|,\quad P=q+b,
\]

\[
T=R[0:j],\quad U=R[j:q],\quad Y=BR=BTU,
\]

\[
E=LRT \xrightarrow{\,U\,} G=LR^2,\qquad
F=LR^2BT \xrightarrow{\,U\,} H=LR^2BR.
\]

Explicitly enumerate the proper intermediate prefixes on both paths.
Exclude terminal `H` from any conclusion asserting a period at least `P`,
because `H` already has canonical period `P` by hypothesis. State whether `G`
is included.

Then adversarially audit the statement for:

- generated versus preloaded copies;
- maximal curling number versus a displayed lower-bound power;
- shortest maximizing period;
- endpoint and interval conventions;
- whether every strict-record hypothesis is actually available.

## Immediate proof target

Close the three remaining cells:

1. external later cube with `r=q`;
2. external later cube with `q<r<P`;
3. internal later cube.

Prioritize the fact that the same word `U` is actually generated in both
parallel windows. Static Fine–Wilf geometry alone admits explicit impostors.

For each numerical search:

- calibrate against total lengths `5,66,142`;
- check every suffix block length;
- report cutoffs and capped trajectories;
- distinguish final-copy-generated from entirely generated powers;
- save executable source and exact output.

## Downstream work

Bridge promotion is not the full theorem. If it is proved, continue through
replay stability, first return, bridge-period monotonicity, finite square
chains, record-cube recurrence, autonomous exact-power termination, lifting
to arbitrary integer alphabets, and full Lean/adversarial verification.

