# Approach-family registry

Chronological decisions, corrections, and admissible proof status are recorded
in `research_ledger.md`.  This table is a current-state index, not a substitute
for that ledger or for the full proofs in the linked topic notes.

This registry tracks proof families for the current hypothetical-counterorbit
reduction.  `OPEN` means that a concrete lemma remains under active attack;
`BLOCKED` means that an explicit counterexample or theorem-strength gap is
recorded in the linked note.  Nothing in this file is a proof of the Curling
Number Conjecture.

| Family | Status | Strongest rigorous output | Exact remaining gap |
|---|---|---|---|
| Golden local periods / record roots | OPEN | Unbounded golden periods and cube-root records; all sufficiently large new cube-root records have label 3 | Fixed-origin square/cube tower and moving context |
| Power-tower origin descent | OPEN | Nonself first divergences move origin left; minimal-origin levels become exact self-replayers | Classify or destroy unbounded square/cube self-replayers |
| Autonomous-one anchor descent | PROVED SUBLEMMA | Any unbounded family of `cn=1` factors at one fixed seed anchor descends the anchor to zero | Produce such a fixed-anchor family; bounded-sink cascades remain |
| Border / reset normalization | BLOCKED | Short bridges halve a reset to a proper-border reset; sparse all-depth masking is possible | Intermediate orbit equations must rule out bounded sinks |
| Early square replay descent | BLOCKED | Exact `q=1,2` overlap split and strict smaller defects in most branches | Contexted-defect closure is false; source-high hidden powers remain |
| Circular proper-curl fixed profiles | OPEN | Two copies detect all proper roots; min symbol 2; largest root among labels >=3 has label 3 | General maximum label, and classification of fixed profiles |
| Binary cube-run / transition grammar | OPEN | Tight cube gadgets; a globally maximal root of marker span `s` forces every internal defect child of span `t` to satisfy `2t+gcd(s,t)<=s`.  Containment below a globally maximal fitting cube repairs fitting throughout that halving hierarchy.  The unrestricted cyclic cube-equality lemma forces every surviving ancestry cycle to contain a root-one/`2` external source.  A selected unary external cube is exactly the gap-four pair `222322232`, and two selected unary external edges cannot be consecutive; after pair contraction its complete transition alphabet is root-four return, adjacent-`33`, or long pair-parent copy, with the critical-origin arc retained.  Q21 disproves single-container SCC attachment: its unique sink alternates two maximal root-four cube intervals overlapping in one symbol, with contained root-one descents and escaping external ascents.  Near-model SCCs show unary and nonterminal external sources survive every on-SCC equation while only off-SCC exactness fails | Eliminate the adjacent-`33` / pointed border-loss exits of the contracted pair automaton, or prove that every surviving escape chain is the Q21 two-container macro; a laminar one-container saturation lemma is false |
| Cubic-run handles / variation | BLOCKED AS STANDALONE | Oriented Hall property checked through length 25 | CK handles alone miss exact square-label equations; q114 near-model |
| Minimal-square midpoint graph | BLOCKED AS STANDALONE | Same-color uniqueness fails at admissible length 22, and mixed weighted injectivity fails at exact length 37.  At an all-external child the parent-tail match is at most one symbol; the shortest phase-zero root has autonomous value one and forces a robust internal square reset.  Iterating these resets either lands at the old delimiter or produces an explicit parent `3/2` completion seam. | Eliminate the crossing completion seam using orbit provenance.  The exact Q21 midpoint cycle has roots `4,6,4,7` and edge deficits `8,2,10,1`, so root length, excess, and winding count are not descent ranks; the one-symbol tail seam does not control the copied filler prefix. |
| Symbolic square-root word equation | BLOCKED | Q21 itself has no required greedy equation under any rotation/reversal | No bridge from midpoint cycles to consecutive-square factorization |
| Maximum-label return quotient | BLOCKED AS STATED | A raw `k`-power at a terminal entrance marker gives only `k-1` aligned return-word gaps unless its unmatched initial boundary is also a marker; Q21 and the length-37 weighted profile execute this loss | Classify the pointed initial partial return, or independently force the power start to be a marker; prior weighted-lift exclusions do not exclude max-4 profiles |
| Weighted squareful profiles | BLOCKED AS STANDALONE | Exact ternary all-weight-2 model at length 31 and mixed-weight model at length 37 | Abstract weighted profiles are genuinely many-colored; only lift-specific constraints remain |
| Uniform finite equality types | OPEN | Fixed starting length has a uniform finite termination bound if every seed terminates | Does not control unbounded seed length |
| Shortest-seed deletion | BLOCKED AS STANDALONE | First divergence is exactly a whole-word primitive `k`-power completing `k-1 -> k` | Repeated divergences are the already unresolved fixed-origin power tower |
| Minimum deleted hitting time | OPEN | A counterexample implies an immediate primitive whole-power split `A=Y^k`, `cn(A[1:])=k-1`; while driven deletion stays terminating, equality steps lower its rank and strict steps are nested whole-prefix power resets with nondecreasing roots; if deletion becomes bad, it does so immediately after a reset and first shares the next output.  For consecutive late cubic resets `U -> V`, the exact rank transport is `tau((U^3)[1:]3)=3(|V|-|U|)+tau(rot(V)^3)`.  Also, primitive `U` cannot make `U^3 3 2` a whole power | Prove the actual-external-ancestry local death equations `cn((U^3)[1:]3)=2` and `cn((U^3)[1:]32)=1`, or replace them with another well-founded transport.  Critical fixedness/fitting alone is false; full parent orbit ancestry is load-bearing |
| Essential endpoint rank | OPEN | Consecutive rotation cubes have constant status across every phase labelled at least `3`, so every residual boundary is at a `2`.  At that seam the three-word insertion has only a contained completion fork or a prefix seam.  The locked unique-`2` branch is impossible.  Endpoint rank is conserved in the ordinary external branch.  The long subcase inherits `pc_Y=Y` directly; in the short subcase the border translation `P[:s]P[:t]=P[:s+t]` imports every missing early proper square, so it too inherits `pc_Y=Y`.  A minimum-rank/maximum-period tie-break therefore eliminates the entire ordinary external prefix alternative.  At the remaining contained fork, deleting the leading `2` preserves the promoted value.  If that deletion terminates, its bad/terminal prefix mismatch inflates to a new exact-profile cubic boundary with period more than double and conserves the new endpoint rank; if it is bad, the `D3`/`D2` completion defect moves left and its terminal endpoint is one below the original rank.  Repeating the terminal branch yields nested `F_i`, but each terminal orbit leaves at the next forced `3/2` defect; ranks transport to the next ordinary boundary and centered maximizing roots escape every finite window.  The successive roots are exactly consecutive fixed-origin resets, so every step is either `r>3n` with literal `C^3 3` nesting or `r=3n-a` with one bounded seed border deleted | Couple the descended same-prefix-completion rank to the forward/reverse deletion ranks, or prove a genuinely dynamical relation between successive exact profiles/tails that excludes the two known consecutive-reset mechanisms.  Prefix nesting, coarse endpoint ranks, compactness, and the golden-ratio periodicity threshold do not close either object-type transition |
| Reverse-status prefix reset | OPEN | Any terminal superword containing a bad suffix yields a minimum-terminal-time adjacent pair `A=aB` with `A=Y^k` terminal, `B` bad, and exact values `k/(k-1)`, where `k>=3`.  Minimality forces the adjacent completion fork `B(k-1)` bad versus `Bk` terminal; nontrivial roots at the fork obey exact Fine--Wilf separation.  Rotation-cube status is constant across labels at least three.  At a symbol-`2` rotation boundary the reverse normalization is `D2=Q^3` bad versus `D3` terminal, dual to the ordinary fork above | Relate the reverse minimum-terminal-prefix rank to the ordinary minimum-deleted-time rank by an explicit orbit identity; the current critical self-replay branch remains conditional, and root drop alone is not iterable |
| Bounded internal-reset defects | OPEN | Consecutive internal defects lie at distinct seed cuts.  A decreasing defect gives a strictly shorter maturation carrier.  In the increasing case, phase locking plus an explicit equality-graph argument eliminates every crossing cube with root `2r>|U|`; the surviving rescue has `d<r<|U|/2` and forces `U=K(EF)^2` | Turn the strict half-scale suffix rescue into a closed visible carrier/rank, or show that the bounded hidden prefix `|K|<N_0` cannot recur indefinitely |
| Post-promotion phase synchronization | OPEN | Critical root has `P[0]=2` and promotes `P^3 -> P^3 3`; every maximizing root during the first `|P|` surviving post-promotion cuts is strictly shorter than `|P|`; the first lost circular phase is an exact one-letter period-completion defect `Zb -> Zk`; a common suffix of length `2|P|` locks the phase while roots stay short | Exclude an indefinite cascade of completion defects using the full equation `pc_P=P`; local length/exponent descent is false |
| Minimum-root terminal prefix | CONDITIONAL ONLY | If the first-divergence replay root has exactly the globally minimum counterseed length, deleting its last symbol gives the strict terminal drop and the canonical `AVA` / `XBXBX` branches; several quotient branches are now normalized or eliminated | The replay root can be a strictly longer orbit state.  Then `P[:-1]` is its immediate predecessor and `cn(P[:-1])=P[-1]`, so minimum-seed strictness does not transfer.  The generic proof must first eliminate this `|P|>|S|` branch. |
| Local reset-root descent | BLOCKED AS STANDALONE | At a first factor loss with next exponent two, the least reset root has autonomous curling number one and length below `|P|` | A one-phase near-model with `pc(P)` wrong at exactly one cut shrinks to root length one and then regrows to six; neither root length nor `(length, exponent)` is well founded without the missing global profile equation |
| Seed-anchored circular parent cycles | BLOCKED AS STANDALONE | Every genuine cycle is seed-anchored with bounded winding; nevertheless `A_r=2^(r-2)32`, `Q_L=A_3...A_(L+2)` gives cycle-local exact least-root/self-label cycles of arbitrary length, winding one, and fixed seed bound `N=8` | The countermodels fail `pc_Q(d)=Q[d]` off-cycle, so only the global fixed-profile equations can still force compression |
| One-sided threshold exit ancestry | OPEN | Every late `H_k` component for `k>=3` has length below the fixed cutoff `N`; every late maximizing root at a label at least four is therefore shorter than `N`.  Shadow divergence transfers a label-two root `q` to a high-label root `>q/M`, so label-three maximizing roots are unbounded.  Each sufficiently long label-three cube has the exact pointed normal form `(UV)^3=U(VU)^2V` with bounded `|U|` and a return-aligned middle square; its scale also appears on an earlier exact label-two maximizing edge | Close or strictly descend the bounded pointed overhang using actual intervening self-label equations.  Finite return types and stabilized suffix-copy ancestry alone admit an aperiodic encoded-Fibonacci countermodel |
| Recurrence / entropy / S-adic towers | BLOCKED AS STANDALONE | Explicit Fibonacci countermodel has nested primitive cubes, recurrence, fixed delimiter, and no fourth powers; Mignosi--Restivo--Salemi's `phi^2` threshold cannot be inferred at label-two cuts, and Q21 has an exact-profile/fitting replay cut of maximum rational suffix exponent exactly two | Must use exact self-label maximality together with cross-level orbit ancestry; delimiter geometry and squarefulness alone are exhausted |
| Primitive self-labelled profile classification | OPEN | Exact solving finds the binary proper-profile/fitting word only in the length-21 `Q21` rotation class through length 50; all exact hidden children of the length-21 models fail.  Searches requiring a maximum symbol at least four find no model through the recorded finite ranges | Turn the finite signal into a uniform theorem; periodic optimal-profile words are outside published aperiodic squareful classifications, and the general maximum-symbol seam quotient is still defective |
